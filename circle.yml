dependencies:
    override:
        - pip install -r requirements.txt
        - pip install -r requirements-dev.txt
    post:
        - python -m nltk.downloader punkt
test:
    override:
        - py.test serapis/tests/
